🎵 Ultimate Party Bot - บอทเพลง Discord สายปาร์ตี้

บอทเพลง Discord ที่เขียนด้วย Python เน้นใช้งานง่าย ไม่กระตุก พร้อมฟีเจอร์ระดับเทพอย่าง การข้ามเวลา (Seek), ปรับเสียง (Filters), เนื้อเพลง (Lyrics) และระบบ Auto Lo-Fi แก้เหงาเมื่อไม่มีใครขอเพลง

✨ ฟีเจอร์เด่น

🎧 เล่นเพลงลื่นๆ: รองรับ YouTube, YouTube Playlist (โหลดไวมาก)

⏯️ ควบคุมง่าย: มีปุ่มกด Play, Pause, Skip, Loop ครบ

⏩ Seek ข้ามเวลา: ข้ามไปข้างหน้า 30 วิ หรือถอยหลัง 10 วิ ได้ทันที

🎚️ Audio Filters: เปลี่ยนโหมดเสียงเป็น Bass Boost, Nightcore, Slowed ได้แบบ Real-time

📜 Lyrics: กดปุ่มดูเนื้อเพลงได้ทันที (ดึงจาก Genius)

☕ 24/7 Lo-Fi Mode: เมื่อคิวเพลงหมด บอทจะเปิดเพลง Lo-Fi คลอไปเรื่อยๆ ไม่เงียบเหงา

🗑️ Easy Queue: มีปุ่มล้างคิว และปุ่มดูคิวทั้งหมด

🛠️ สิ่งที่ต้องมีก่อน (Pre-requisites)

ก่อนติดตั้ง ต้องมั่นใจว่าเครื่องคุณมี 3 อย่างนี้:

Python (เวอร์ชัน 3.8 ขึ้นไป)

Node.js (สำคัญมาก! ช่วยให้ค้นหาเพลงเร็วและไม่เอ๋อ) -> ดาวน์โหลดที่นี่ (ลงตัว LTS ก็พอ)

FFmpeg (ตัวขับเคลื่อนเสียง ขาดไม่ได้) -> อ่านวิธีลงด้านล่าง

📥 วิธีติดตั้ง (Installation)

1. เตรียมโปรเจกต์

เปิด Terminal (CMD หรือ PowerShell) แล้วพิมพ์คำสั่ง:

git clone <ลิงก์ GitHub ของคุณ>
cd botdis


2. ติดตั้ง FFmpeg 

เจ้าตัวนี้คือเครื่องยนต์ของบอทครับ ต้องมีไม่งั้นบอทใบ้กิน วิธีลงง่ายๆ:

ไปดาวน์โหลดที่เว็บ gyan.dev (เลือกคลิกที่ลิงก์ ffmpeg-git-full.7z)

แตกไฟล์ออกมา (Unzip) แล้วเข้าไปในโฟลเดอร์ bin

หาไฟล์ชื่อ ffmpeg.exe (มีไอคอนสีดำๆ)

ลากไฟล์ ffmpeg.exe มาวางไว้ในโฟลเดอร์เดียวกับ main.py

จบ! ไม่ต้องตั้งค่า Path อะไรเพิ่มแล้ว

3. ติดตั้ง Library (ทีเดียวจบ)

พิมพ์คำสั่งนี้ใน Terminal เพื่อลงส่วนเสริมที่จำเป็นทั้งหมด:

pip install -r requirements.txt


4. ตั้งค่ารหัสลับ (.env)
สร้างไฟล์ใหม่ชื่อ .env (มีจุดข้างหน้า) ไว้ที่เดียวกับ main.py
เปิดไฟล์แล้ววางโค้ดนี้ลงไป (ใส่รหัสของคุณเอง):
DISCORD_TOKEN="เอา_TOKEN_บอท_มาใส่ตรงนี้"
GENIUS_TOKEN="เอา_TOKEN_GENIUS_มาใส่ตรงนี้"


🚀 วิธีรันบอท (Start)
python main.py หรือ กดที่ Start
